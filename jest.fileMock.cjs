// Stub for static asset imports (png/svg/etc.) in Jest. Vite resolves these to
// a URL string at build time; tests only need a stable, truthy stand-in so
// components that render <img src={imported}> mount without a real binary.
module.exports = 'test-file-stub';
