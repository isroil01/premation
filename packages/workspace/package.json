{
  "name": "@motion/workspace",
  "version": "1.0.0",
  "description": "Framework-independent Workspace Engine — the interaction layer between the user and the Scene Graph (viewport, camera, tools, selection, snapping, hit-testing).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "jest --config jest.config.cjs"
  },
  "license": "MIT"
}
