{
  "name": "@motion/timeline",
  "version": "1.0.0",
  "description": "Framework-independent Timeline Engine — the data engine that manages all temporal data (tracks, layers, clips, markers, playhead, ranges) for the motion editor.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "jest --config jest.config.cjs"
  },
  "license": "MIT"
}
