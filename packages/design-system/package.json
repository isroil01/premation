{
  "name": "@motion/design-system",
  "version": "1.0.0",
  "description": "Authored static design: OKLCH colour, layout grid, type scale, layered depth, surface treatment, shape vocabulary — plus the layout-template library. Emits ToolCall[]; executes nothing.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "jest --config jest.config.cjs"
  },
  "license": "MIT"
}
