{
  "name": "@motion/audio",
  "version": "1.0.0",
  "description": "Beat, tempo and onset detection. Pure PCM in, times out — no Web Audio, no DOM.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false,
  "scripts": { "typecheck": "tsc --noEmit -p tsconfig.json", "test": "jest --config jest.config.cjs" },
  "license": "MIT"
}
