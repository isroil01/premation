{
  "name": "@motion/technique-library",
  "version": "1.0.0",
  "description": "Hand-authored motion techniques. The LLM casts; this emits. Deterministic ToolCall[] with a craft floor that does not depend on the model.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "jest --config jest.config.cjs"
  },
  "license": "MIT"
}
