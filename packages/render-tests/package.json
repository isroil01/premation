{
  "name": "@motion/render-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Golden-frame regression suite (Phase 0) — deterministic pixel diffing of the render engines.",
  "scripts": {
    "render-tests": "node scripts/run.mjs",
    "update": "node scripts/run.mjs --update"
  }
}
