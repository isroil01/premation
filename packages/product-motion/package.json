{
  "name": "@motion/product-motion",
  "version": "1.0.0",
  "description": "Product/UI motion — a separate discipline from editorial. Springs, not beziers; 200-300ms, not 400-900; shared-element transitions; a real component kit.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false,
  "scripts": { "typecheck": "tsc --noEmit -p tsconfig.json", "test": "jest --config jest.config.cjs" },
  "license": "MIT"
}
