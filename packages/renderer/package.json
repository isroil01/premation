{
  "name": "@motion/renderer",
  "version": "1.0.0",
  "description": "Framework-independent, backend-abstracted rendering engine for the AI-native motion editor (WebGPU primary, WebGL2 fallback).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "jest --config jest.config.cjs"
  },
  "license": "MIT"
}
