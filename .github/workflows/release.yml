# Cut a release: build SIGNED installers for Windows and macOS and attach them to
# a GitHub release that users download from — and that installed apps read to
# discover updates.
#
#   npm run release:patch        # bumps package.json, commits, tags v0.1.1
#   git push --follow-tags       # <- this is what starts the workflow
#
# Then in GitHub ▸ Releases a DRAFT is waiting. Write the notes, press Publish.
# No installed app sees the update until you do (electron-builder.yml:
# `releaseType: draft`).
#
# ── RELEASE POLICY, enforced below rather than documented ────────────────────
#  1. Releases come from `main` ONLY. A tag that is not an ancestor of `main`
#     fails the pipeline. No release from `dev` or a feature branch.
#  2. Flow: feature branch → dev → main → tag → release.
#  3. Artifacts are built from the TAGGED COMMIT, never a working tree.
#  4. An unsigned artifact is not publishable. If signing or notarization fails,
#     the release fails — there is no unsigned fallback.
#
# Rule 4 is the one worth restating: the tempting move at 2am is to publish
# unsigned "just this once", and the point of the pipeline is that it cannot.
name: release

on:
  push:
    tags:
      - 'v*'
  # Manual runs rehearse the build. They produce no release, so a dry run can
  # never publish something half-finished.
  workflow_dispatch:

permissions:
  contents: write

jobs:
  verify:
    name: Verify tag, branch, types and tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # Full history: the ancestry check needs `main`, and a shallow clone
          # would make every tag look unreachable from it.
          fetch-depth: 0

      # ── POLICY GATE 1: the tag must be on `main` ──
      #
      # A tag pushed from `dev` or a feature branch builds and publishes exactly
      # as convincingly as a real release — the installer is real, the update
      # manifest is real, and users get it. Nothing downstream can tell the
      # difference, so this is the only place that can refuse.
      - name: Tag must be an ancestor of main
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          git fetch --no-tags origin +refs/heads/main:refs/remotes/origin/main
          if ! git merge-base --is-ancestor "$GITHUB_SHA" origin/main; then
            echo "::error::Tag $GITHUB_REF_NAME ($GITHUB_SHA) is not an ancestor of main."
            echo "::error::Releases are cut from main only. Flow: feature -> dev -> main -> tag."
            echo "::error::Merge to main first, then tag the merge commit."
            exit 1
          fi
          echo "OK: $GITHUB_REF_NAME is on main."

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      # ── POLICY GATE 2: tag matches package.json ──
      # electron-builder reads the version from package.json and writes it into
      # latest.yml. If the two disagree the release is called v1.2.3 while the
      # update metadata says 1.2.2, and every client either misses the update or
      # installs it forever.
      - name: Tag matches package.json version
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          TAG="${GITHUB_REF_NAME#v}"
          PKG="$(node -p "require('./package.json').version")"
          echo "tag=$TAG package.json=$PKG"
          if [ "$TAG" != "$PKG" ]; then
            echo "::error::Tag $GITHUB_REF_NAME does not match package.json version $PKG."
            echo "::error::Bump with 'npm run release:patch' rather than tagging by hand."
            exit 1
          fi

      # ── POLICY GATE 3: signing credentials must exist ──
      # Checked BEFORE the long build, so a missing secret fails in seconds
      # rather than after two platform builds have run.
      - name: Signing credentials are configured
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          MAC_CSC: ${{ secrets.MAC_CSC_LINK }}
          MAC_PW: ${{ secrets.MAC_CSC_KEY_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PW: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM: ${{ secrets.APPLE_TEAM_ID }}
          WIN_CSC: ${{ secrets.WIN_CSC_LINK }}
          WIN_PW: ${{ secrets.WIN_CSC_KEY_PASSWORD }}
        run: |
          missing=""
          for v in MAC_CSC MAC_PW APPLE_ID APPLE_PW APPLE_TEAM WIN_CSC WIN_PW; do
            [ -z "${!v}" ] && missing="$missing $v"
          done
          if [ -n "$missing" ]; then
            echo "::error::Missing signing secrets:$missing"
            echo "::error::A release cannot publish unsigned artifacts. See RELEASING.md 'Code signing'."
            exit 1
          fi
          echo "OK: all signing secrets present."

      - run: npm run typecheck
      - run: npx tsc -p electron/tsconfig.json --noEmit
      - run: npm test -- --ci

  build:
    name: Build ${{ matrix.name }}
    needs: verify
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            name: Windows
            args: --win            # NSIS → Premation-Setup-Windows.exe
          - os: macos-latest
            name: macOS
            # BOTH architectures in ONE job, deliberately: two jobs would each
            # publish their own latest-mac.yml and the second would overwrite the
            # first, leaving one architecture unable to update.
            args: --mac --x64 --arm64
          # LINUX IS DELIBERATELY ABSENT — removed, not commented out.
          # See RELEASING.md "Platform support".

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      - name: Build renderer and main process
        shell: bash
        run: |
          if [ -n "${{ vars.VITE_BACKEND_ORIGIN }}" ]; then export VITE_BACKEND_ORIGIN="${{ vars.VITE_BACKEND_ORIGIN }}"; fi
          if [ -n "${{ vars.VITE_MEDIA_ORIGINS }}" ]; then export VITE_MEDIA_ORIGINS="${{ vars.VITE_MEDIA_ORIGINS }}"; fi
          npm run electron:build

      - name: Warn if no backend origin was configured
        if: ${{ vars.VITE_BACKEND_ORIGIN == '' }}
        shell: bash
        run: echo "::warning::VITE_BACKEND_ORIGIN is not set — these installers point at http://localhost:4000."

      # ── Package + sign ──
      # electron-builder reads CSC_LINK / CSC_KEY_PASSWORD from the environment.
      # Notarization is done explicitly below rather than by electron-builder, so
      # submission, stapling and verification are separate inspectable steps.
      - name: Package and sign
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_LINK: ${{ matrix.os == 'macos-latest' && secrets.MAC_CSC_LINK || secrets.WIN_CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ matrix.os == 'macos-latest' && secrets.MAC_CSC_KEY_PASSWORD || secrets.WIN_CSC_KEY_PASSWORD }}
        run: npx electron-builder ${{ matrix.args }} --publish never

      # ── macOS: notarize, then STAPLE ──
      # Stapling matters more than it looks. Without the ticket attached, a
      # machine that is offline — or behind a filter that blocks Apple's
      # verification endpoints — cannot check the notarization and Gatekeeper
      # blocks the app, on a build that IS notarized.
      - name: Notarize and staple (macOS)
        if: matrix.os == 'macos-latest'
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          set -euo pipefail
          shopt -s globstar nullglob
          for dmg in release/**/*.dmg; do
            echo "-- notarizing $dmg"
            xcrun notarytool submit "$dmg" \
              --apple-id "$APPLE_ID" \
              --password "$APPLE_APP_SPECIFIC_PASSWORD" \
              --team-id "$APPLE_TEAM_ID" \
              --wait
            xcrun stapler staple "$dmg"
          done

      # ── FAIL-CLOSED VERIFICATION, before upload ──
      # Without this the pipeline can succeed with a silently unsigned build:
      # electron-builder does NOT fail when CSC_LINK is empty, it just skips
      # signing and carries on.
      - name: Verify macOS artifacts are signed, notarized and stapled
        if: matrix.os == 'macos-latest'
        run: |
          set -euo pipefail
          shopt -s globstar nullglob
          found=0
          for dmg in release/**/*.dmg; do
            found=1
            echo "-- verifying $dmg"
            spctl -a -vvv --type install "$dmg"   # rejects unsigned/improperly signed
            xcrun stapler validate "$dmg"          # rejects a missing ticket
          done
          [ "$found" = 1 ] || { echo "::error::No .dmg produced"; exit 1; }

      - name: Verify Windows artifacts are signed and timestamped
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          $found = $false
          Get-ChildItem -Path release -Recurse -Filter *.exe | ForEach-Object {
            $found = $true
            Write-Host "-- verifying $($_.FullName)"
            $sig = Get-AuthenticodeSignature $_.FullName
            Write-Host "   status: $($sig.Status)"
            if ($sig.Status -ne 'Valid') {
              Write-Error "Unsigned or invalid signature: $($_.Name) [$($sig.Status)]"
              exit 1
            }
            # A signature without a timestamp stops validating the day the
            # certificate expires, retroactively breaking every build shipped.
            if (-not $sig.TimeStamperCertificate) {
              Write-Error "Signature is not RFC-3161 timestamped: $($_.Name)"
              exit 1
            }
          }
          if (-not $found) { Write-Error "No .exe produced"; exit 1 }

      # Publish only AFTER verification passed, and only on a tag.
      - name: Publish to the draft release
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_LINK: ${{ matrix.os == 'macos-latest' && secrets.MAC_CSC_LINK || secrets.WIN_CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ matrix.os == 'macos-latest' && secrets.MAC_CSC_KEY_PASSWORD || secrets.WIN_CSC_KEY_PASSWORD }}
        run: npx electron-builder ${{ matrix.args }} --publish always

      - name: Upload installers as workflow artifacts
        uses: actions/upload-artifact@v4
        with:
          name: installers-${{ matrix.name }}
          path: |
            release/**/*.exe
            release/**/*.dmg
            release/**/*.yml
            release/**/*.blockmap
          if-no-files-found: error
          retention-days: 14
