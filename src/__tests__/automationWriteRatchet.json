{
  "comment": "B5 ratchet (docs/ENGINE_API.md §15.6). Counts may only go DOWN; lower them with `node scripts/lint/automationWritesReport.mjs --update` after moving writes onto engine commands.",
  "total": 133,
  "areas": {
    "ai": 92,
    "plugins": 41,
    "scripts/automation": 0
  }
}
