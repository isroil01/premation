# CMake preset binary dirs (native/build/<preset>) and cmake-js output.
build/
bindings/napi/build/
bindings/napi/node_modules/
bindings/napi/package-lock.json
# vcpkg manifest-mode install tree lives inside build/, but a stray
# vcpkg_installed/ from a manual `vcpkg install` should not be committed either.
vcpkg_installed/
# C1 prototype measurement output (native/engine/proto-host/run-matrix.mjs default --out).
engine/proto-host/.results/
