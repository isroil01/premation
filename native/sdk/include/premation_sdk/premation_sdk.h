/*
 * Premation native plugin SDK — include this one header.
 *
 *   pr_types.h    versioning, PrErr, handles, time
 *   pr_world.h    CPU pixel worlds (8 / 16 / 32-bit float)
 *   pr_params.h   the declarative parameter model
 *   pr_gpu.h      the GPU path (the engine's Dawn / WebGPU device and textures)
 *   pr_host.h     the host callback suite
 *   pr_effect.h   the entry point, command selectors, in/out data, flags
 *
 * Guide: docs/PLUGIN_SDK.md. Samples: native/sdk/samples/.
 */
#ifndef PREMATION_SDK_H
#define PREMATION_SDK_H

#include "pr_effect.h"
#include "pr_gpu.h"
#include "pr_host.h"
#include "pr_params.h"
#include "pr_types.h"
#include "pr_world.h"

#endif /* PREMATION_SDK_H */
